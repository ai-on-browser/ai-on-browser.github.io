import PolynomialInterpolation from"../../lib/model/polynomial_interpolation.js";var dispPolynomialInterpolation=function(t,n){t.append("input").attr("type","button").attr("value","Fit").on("click",(()=>{const t=new PolynomialInterpolation;t.fit(n.trainInput.map((t=>t[0])),n.trainOutput.map((t=>t[0])));const i=t.predict(n.testInput(1).map((t=>t[0])));n.testResult(i)}))};export default function(t){t.setting.ml.usage='Click and add data point. Next, click "Fit" button.',t.setting.ml.require={dimension:1},dispPolynomialInterpolation(t.setting.ml.configElement,t)}