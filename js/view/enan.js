import ENaN from"../../lib/model/enan.js";import Controller from"../controller.js";export default function(t){t.setting.ml.usage='Click and add data point. Then, click "Calculate".',t.setting.ml.reference={author:"J. Feng, Q. Zhu, J. Huang, L. Yang",title:"Extend natural neighbor: a novel classification method with self-adaptive neighborhood parameters in different stages",year:2016};new Controller(t).input.button("Calculate").on("click",(()=>{const e=new ENaN;e.fit(t.trainInput,t.trainOutput.map((t=>t[0])));const n=e.predict(t.testInput(4));t.testResult(n)}))}