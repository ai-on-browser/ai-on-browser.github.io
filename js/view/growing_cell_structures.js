import GrowingCellStructures from"../../lib/model/growing_cell_structures.js";import Controller from"../controller.js";var dispGrowingCellStructures=function(t,e){const n=new Controller(e);let r=null;n.stepLoopButtons().init((()=>{r=new GrowingCellStructures,t.select("[name=clusternumber]").text(r.size+" clusters"),e.init()})).step((n=>{r.fit(e.trainInput);const l=r.predict(e.trainInput);e.trainResult=l.map((t=>t+1)),e.centroids(r._nodes,r._nodes.map(((t,e)=>e+1)),{line:!0,duration:10});const s=r.predict(e.testInput(4));e.testResult(s.map((t=>t+1))),t.select("[name=clusternumber]").text(r.size+" clusters"),n&&setTimeout(n,10)})),t.append("span").attr("name","clusternumber").style("padding","0 10px").text("0 clusters")};export default function(t){t.setting.ml.usage='Click and add data point. Next, click "Add centroid" to add centroid. Finally, click "Step" button repeatedly.',dispGrowingCellStructures(t.setting.ml.configElement,t)}