import RDF from"../../lib/model/rdf.js";import Controller from"../controller.js";export default function(e){e.setting.ml.usage='Click and add data point. Then, click "Calculate".',e.setting.ml.reference={author:" D. Ren, B. Wang, W. Perrizo",title:"RDF: a density-based outlier detection method using vertical data representation",year:2004};const t=new Controller(e),n=()=>{const t=new RDF(a.value).predict(e.trainInput);e.trainResult=t.map((e=>e>l.value))},a=t.input.number({label:" Radius ",min:0,max:100,step:.1,value:.1}).on("change",n),l=t.input.number({label:" Threshold ",min:0,max:10,step:.1,value:2}).on("change",n);t.input.button("Calculate").on("click",n)}