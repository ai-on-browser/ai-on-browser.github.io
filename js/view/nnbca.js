import NNBCA from"../../lib/model/nnbca.js";import Controller from"../controller.js";export default function(t){t.setting.ml.usage='Click and add data point. Then, click "Calculate".',t.setting.ml.reference={author:"J. Feng, Y. Wei, Q. Zhu",title:"Natural Neighborhood-Based Classification Algorithm Without Parameter k",year:2018};new Controller(t).input.button("Calculate").on("click",(()=>{const e=new NNBCA;e.fit(t.trainInput,t.trainOutput.map((t=>t[0])));const n=e.predict(t.testInput(4));t.testResult(n.map((t=>t??-1)))}))}