import ODIN from"../../lib/model/odin.js";import Controller from"../controller.js";export default function(e){e.setting.ml.usage='Click and add data point. Then, click "Calculate".',e.setting.ml.reference={author:"V. Hautamaki, I. Karkkainen, P. Franti",title:"Outlier detection using k-nearest neighbour graph",year:2004};const n=new Controller(e),t=()=>{const n=new ODIN(a.value,l.value).predict(e.trainInput);e.trainResult=n},a=n.input.number({label:" k = ",min:1,max:100,value:5}).on("change",t),l=n.input.number({label:" t = ",min:0,max:100,value:1}).on("change",t);n.input.button("Calculate").on("click",t)}