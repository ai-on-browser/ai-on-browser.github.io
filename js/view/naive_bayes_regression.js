import NaiveBayesRegression from"../../lib/model/naive_bayes_regression.js";import Controller from"../controller.js";export default function(e){e.setting.ml.usage='Click and add data point. Then, click "Fit".',e.setting.ml.reference={author:"E. Frank, L. Trigg, G. Holmes, I. H. Witten",title:"Naive Bayes for Regression",year:1999};new Controller(e).input.button("Fit").on("click",(()=>{const t=e.trainInput,n=new NaiveBayesRegression(Array(t[0].length).fill(!1));n.fit(t,e.trainOutput.map((e=>e[0])));const i=n.predict(e.testInput(20));e.testResult(i)}))}