import LogarithmicInterpolation from"../../lib/model/logarithmic_interpolation.js";var dispLI=function(t,i){t.append("input").attr("type","button").attr("value","Calculate").on("click",(function(){let t=new LogarithmicInterpolation;t.fit(i.trainInput.map((t=>t[0])),i.trainOutput.map((t=>t[0])));const n=t.predict(i.testInput(1).map((t=>t[0])));i.testResult(n)}))};export default function(t){t.setting.ml.usage='Click and add data point. Then, click "Calculate".',t.setting.ml.require={dimension:1},dispLI(t.setting.ml.configElement,t)}