import DelaunayInterpolation from"../../lib/model/delaunay_interpolation.js";var dispDelaunay=function(t,n){t.append("input").attr("type","button").attr("value","Calculate").on("click",(function(){let t=new DelaunayInterpolation;t.fit(n.trainInput.map((t=>[t[0],t[1]])),n.trainOutput.map((t=>t[0])));const a=t.predict(n.testInput(3).map((t=>[t[0],t[1]])));n.testResult(a.map((t=>t??-1)))}))};export default function(t){t.setting.ml.usage='Click and add data point. Then, click "Calculate".',t.setting.ml.require={dimension:2},dispDelaunay(t.setting.ml.configElement,t)}