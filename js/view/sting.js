import STING from"../../lib/model/sting.js";var dispSTING=function(elm,platform){const fitModel=cb=>{platform.fit(((tx,ty,pred_cb)=>{const model=new STING;model.fit(tx);cb&&cb()}))};const stepButton=elm.append("input").attr("type","button").attr("value","Fit").on("click",fitModel);elm.append("span").text(" Clusters: ");elm.append("span").attr("name","clusters");return()=>{}};export default function(platform){platform.setting.ml.usage='Click and add data point. Then, click "Fit" button.';platform.setting.terminate=dispSTING(platform.setting.ml.configElement,platform)}