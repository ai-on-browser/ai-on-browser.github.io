import NaiveBayes from"../../lib/model/naive_bayes.js";import Controller from"../controller.js";import{specialCategory}from"../utils.js";export default function(t){t.setting.ml.usage='Click and add data point. Then, click "Calculate".';const e=new Controller(t);let a=new NaiveBayes;const i=()=>{if(a.fit(t.trainInput,t.trainOutput.map((t=>t[0]))),"DE"===t.task){const e=a.probability(t.testInput(3)).map((t=>t.reduce(((t,e)=>t+e),0))),i=Math.min(...e),l=Math.max(...e);t.testResult(e.map((t=>specialCategory.density((t-i)/(l-i)))))}else t.testResult(a.predict(t.testInput(3)))};e.select({label:"Distribution ",values:["gaussian"]}).on("change",i),e.input.button("Calculate").on("click",i)}