import LDF from"../../lib/model/ldf.js";import Controller from"../controller.js";export default function(n){n.setting.ml.usage='Click and add data point. Then, click "Calculate".';const l=new Controller(n),t=()=>{const l=new LDF(e.value).predict(n.trainInput);n.trainResult=l.map((n=>n>a.value))},e=l.input.number({label:" k = ",min:1,max:100,value:5}).on("change",t),a=l.input.number({label:" threshold = ",min:0,max:100,step:.1,value:3}).on("change",t);l.input.button("Calculate").on("click",t)}