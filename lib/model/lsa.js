import{Matrix}from"../util/math.js";export default class LSA{predict(t,r=0){t=Matrix.fromArray(t);const[a,i,o]=t.svd();return a.slice(0,r,1).dot(Matrix.diag(i.slice(0,r))).dot(o.block(0,0,r,r).t).toArray()}}