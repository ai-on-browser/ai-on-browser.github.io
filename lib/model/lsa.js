import Matrix from"../util/matrix.js";export default class LSA{predict(r,t=0){r=Matrix.fromArray(r);const[i,a,o]=r.svd();return i.slice(0,t,1).dot(Matrix.diag(a.slice(0,t))).dot(o.block(0,0,t,t).t).toArray()}}