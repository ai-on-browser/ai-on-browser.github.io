export default class CumSum{constructor(){}predict(datas){let sum=datas[0];for(let i=1;i<datas.length;i++){sum+=datas[i]}const mean=sum/datas.length;const cum=[];let d=0;for(let i=0;i<datas.length;i++){d+=mean-datas[i];cum.push(Math.abs(d))}return cum}}