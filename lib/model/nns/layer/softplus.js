import Layer from"./base.js";import Matrix from"../../../util/matrix.js";export default class SoftplusLayer extends Layer{constructor({beta:t=1,...a}){super(a),this._beta=t}calc(t){return this._i=t,Matrix.map(t,(t=>Math.log(1+Math.exp(this._beta*t))/this._beta))}grad(t){const a=Matrix.map(this._i,(t=>1/(1+Math.exp(this._beta*t))));return a.mult(t),a}toObject(){return{type:"softplus",beta:this._beta}}}SoftplusLayer.registLayer();