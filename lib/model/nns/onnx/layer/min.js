import{onnx as o}from"../onnx_exporter.js";export default{export(n,t){if(!Array.isArray(t.input))throw new Error(`Invalid attribute 'input' value ${t.input}.`);const r=new o.NodeProto;r.setOpType("Min");for(const e of t.input)r.addInput(e);r.addOutput(t.name),n.getGraph().addNode(r)}};
