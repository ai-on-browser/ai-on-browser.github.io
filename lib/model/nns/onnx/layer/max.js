import e from"../onnx_pb.js";export default{export(n,t){if(!Array.isArray(t.input))throw new Error(`Invalid attribute 'input' value ${t.input}.`);const r=new e.NodeProto;r.setOpType("Max");for(const a of t.input)r.addInput(a);r.addOutput(t.name),n.getGraph().addNode(r)}};
