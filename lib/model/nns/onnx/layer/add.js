import{onnx as p}from"../onnx_exporter.js";export default{export(n,t){if(!Array.isArray(t.input))throw new Error(`Invalid attribute 'input' value ${t.input}.`);const r=new p.NodeProto;r.setOpType(t.input.length===2?"Add":"Sum");for(const e of t.input)r.addInput(e);r.addOutput(t.name),n.getGraph().addNode(r)}};
