import{onnx}from"../onnx_importer.js";import{loadTensor}from"../utils.js";export default{import(o,r){const t={};for(const i of o.graph.initializerList)i.name===r.inputList[1]&&(t.slope=loadTensor(i));if(Array.isArray(t.slope))throw new Error(`Invalid slope value ${t.slope}.`);return[{type:"prelu",input:[r.inputList[0]],name:r.outputList[0],a:t.slope}]}};