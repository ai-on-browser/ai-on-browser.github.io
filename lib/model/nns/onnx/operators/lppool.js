import{onnx}from"../onnx_importer.js";import{loadAttribute}from"../utils.js";export default{import(t,e){const s={};for(const t of e.attributeList)s[t.name]=loadAttribute(t);if(s.strides&&s.strides.some((t=>t!==s.strides[0])))throw new Error(`Invalid attribute 'strides' value ${s.strides}.`);if("SAME_UPPER"===s.auto_pad)s.pads=s.kernel_shape.map((t=>Math.ceil((t-1)/2)));else{if(s.auto_pad&&"NOTSET"!==s.auto_pad)throw new Error(`Invalid attribute 'auto_pad' value ${s.auto_pad}.`);if(s.pads){const t=[];for(let e=0;e<s.pads.length/2;e++)t.push([s.pads[e],s.pads[e+s.pads.length/2]]);s.pads=t}}return[{type:"lp_pool",input:[e.inputList[0]],name:e.outputList[0],p:s.p,kernel:s.kernel_shape,padding:s.pads||0,stride:s.strides?s.strides[0]:1,channel_dim:1}]}};