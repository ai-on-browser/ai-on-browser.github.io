import{onnx}from"../onnx_importer.js";import{loadTensor,loadAttribute,requireTensor}from"../utils.js";export default{import(e,t){const i={};for(const e of t.attributeList)i[e.name]=loadAttribute(e);const r={};for(const i of e.graph.initializerList)i.name===t.inputList[1]&&(r.shape=loadTensor(i));if(-1===r.shape[0])r.shape=r.shape.slice(1);else if(r.shape.some((e=>-1===e)))throw new Error(`Invalid shape value ${JSON.stringify(r.shape)}.`);return[...requireTensor(e,t.inputList[0]),{type:"reshape",input:[t.inputList[0]],name:t.outputList[0],size:r.shape}]}};