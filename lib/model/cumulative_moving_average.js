const cumulativeMovingAverage=data=>{const p=[];const d=data[0].length;const s=Array(d).fill(0);for(let i=0;i<data.length;i++){for(let j=0;j<d;j++){s[j]+=data[i][j]}p.push(s.map((v=>v/(i+1))))}return p};export default cumulativeMovingAverage;