import Layer from"./base.js";export default class SoftsignLayer extends Layer{calc(a){return this._i=a,a.copyMap((a=>a/(1+Math.abs(a))))}grad(a){const t=this._i.copyMap((a=>1/(1+Math.abs(a))**2));return t.mult(a),t}}SoftsignLayer.registLayer();